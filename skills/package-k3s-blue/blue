#!/usr/bin/env -S uv run --script
# /// script
# requires-python = ">=3.11"
# dependencies = ["package-k3s-blue"]
#
# [tool.uv.sources]
# package-k3s-blue = { git = "https://github.com/getcolors/k3s.git", rev = "fa4cd21b548bfd295e1ff7d8f993a387d31c9198", subdirectory = "blue" }
# ///
#
# Managed by `bb pin` — do not edit by hand, and never invent a SHA.
#
# Stamped by `bb pin`. K3S_LIB_ROOT=/path/to/k3s still overrides the
# pin with a working tree.
"""Thin launcher for package-k3s-blue.

This file is both the skill payload and the repository entry point —
blue/blue in the repository is a symlink to it. It deliberately holds no
logic of its own: validation, the graph and the steps all live in the
package-k3s-blue library, where the test suite reaches them. A copied
payload is the one place in this project where code cannot be tested, so
nothing that can live elsewhere should live here.
"""

import os
import subprocess
import sys

UNPINNED_ERROR = """\
this launcher carries no k3s pin.
It has not been stamped by `bb pin`, which cannot run until the k3s
repository has been pushed.
Point it at a checkout meanwhile: K3S_LIB_ROOT=/path/to/k3s
(or run through the repository project: cd k3s/blue && uv run python -m package_k3s_blue ...)
"""


def _lib_root_project(root: str) -> str:
    """The uv project inside a K3S_LIB_ROOT checkout: the repository root
    or its blue/ implementation both work."""
    if os.path.isfile(os.path.join(root, "pyproject.toml")):
        return root
    return os.path.join(root, "blue")


def main() -> None:
    root = os.environ.get("K3S_LIB_ROOT")
    if root:
        result = subprocess.run(
            ["uv", "run", "--project", _lib_root_project(root),
             "python", "-m", "package_k3s_blue", *sys.argv[1:]])
        raise SystemExit(result.returncode)
    try:
        from package_k3s_blue import exec  # resolvable only once pinned
    except ImportError:
        sys.stderr.write(UNPINNED_ERROR)
        raise SystemExit(2)
    exec()


if __name__ == "__main__":
    main()
